import React from 'react'
import { storiesOf } from '@kadira/storybook'

<% if (isComp) { -%>
import <%= name %> from './'
<% } else { -%>
import { <%= name %> } from './'
<% } -%>

const baseProps = {}

storiesOf('<%= name %>', module)
  .add('default', () => (
    <<%= name %> {...baseProps} />
  ))
