import React from 'react'
import { storiesOf } from '@kadira/storybook'

<% if (isComp) { -%>
import <%= name %> from './'
<% } else { -%>
import { <%= name %> } from './'
<% } -%>

const stories = storiesOf('<%= name %>', module)

const baseProps = {}

stories
.add('default', () => (
  <<%= name %> {...baseProps} />
))
