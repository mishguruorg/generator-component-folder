import React from 'react'
import { storiesOf, action } from '@kadira/storybook'
import { <%= name %> } from './'

const baseProps = {}

storiesOf('<%= name %>', module)
  .add('default', () => (
    <<%= name %> {...baseProps} />
  ))
